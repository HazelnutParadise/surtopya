## Decisions
- Prefer `data-testid` for key UI controls used by Playwright (sort, load more, card containers).
- Explore pagination uses the existing BFF endpoint `GET /api/surveys/public` with `limit`/`offset`.

